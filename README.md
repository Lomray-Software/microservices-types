# Microservices base typescript interfaces

This package provides base typescript interfaces for microservices.

## Usage

1. Install package:

```sh
  npm i --save-dev @lomray/microservices-types
```
